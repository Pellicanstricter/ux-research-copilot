# Core API
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.11.10
python-multipart==0.0.20
python-dotenv==1.1.1

# LangChain (lightweight versions)
langchain==0.3.27
langchain-openai==0.3.34
langchain-text-splitters==0.3.11
openai==2.1.0

# Vector search (CPU only, no CUDA)
faiss-cpu==1.12.0

# Embeddings (lighter alternative to sentence-transformers with torch)
# Using OpenAI embeddings instead - no additional packages needed

# Redis
redis==6.4.0

# Document processing
python-docx==1.2.0
PyPDF2==3.0.1

# Essential utilities only
numpy==1.26.4